#include <stdlib.h>
#include <stdio.h>
int main(void)
{
	printf("Valor cambiado por: %d\n", rand());
	printf("Valor cambiado por: %d\n", rand());
	printf("Valor cambiado por: %d\n", rand());
	printf("Valor cambiado por: %d\n", rand());
	printf("Valor cambiado por: %d\n", rand());
	printf("Valor cambiado por: %d\n", rand());
}